{
  "name": "smpte-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fan-out sidecar for the SMPTE timecode analyzer. Ingests timecode from the browser over WebSocket and rebroadcasts to subscribers (Phase 1: WS only; OSC/Art-Net to follow).",
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
